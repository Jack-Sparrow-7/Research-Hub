import 'package:flutter/material.dart';

class DomainDesktop extends StatelessWidget {
  const DomainDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}