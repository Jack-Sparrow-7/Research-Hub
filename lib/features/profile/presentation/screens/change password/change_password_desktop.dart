import 'package:flutter/material.dart';

class ChangePasswordDesktop extends StatelessWidget {
  const ChangePasswordDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}