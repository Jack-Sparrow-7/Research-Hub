import 'package:flutter/material.dart';

class DomainMobile extends StatelessWidget {
  const DomainMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox();
  }
}